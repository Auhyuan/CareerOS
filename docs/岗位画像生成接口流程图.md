# 岗位画像生成接口流程图

## 1. 接口

```http
POST /job/profiles/generate
```

该接口通过 `profile_type` 区分用户画像和系统画像生成路线。

## 2. 完整流程

```mermaid
flowchart TD
    request[/"POST /job/profiles/generate"/]
    parse["解析 JobProfileGenerateRequest"]
    typeCheck{"profile_type?"}

    userValidate{"user_id 和 job_text 完整?"}
    invalidParams["统一返回 code=422"]
    systemData{"参考系统岗位数据?"}
    unavailableData["统一返回 code=400"]
    cleanText["清理 job_text"]
    callAgent["调用能力层 Agent"]
    agentSuccess{"Agent 调用成功?"}
    agentError["统一返回 code=502"]
    parseOutput["提取岗位画像 JSON"]
    outputValid{"结构与业务规则有效?"}
    repair["请求 Agent 修复一次"]
    repairValid{"修复结果有效?"}
    invalidOutput["统一返回 code=422"]
    buildProfile["补充 user_id 与 profile_type"]
    saveProfile[("job_market_profiles")]
    success["统一返回 code=0"]

    systemRoute["进入 system 路线"]
    systemUnavailable["统一返回 code=400"]

    request --> parse --> typeCheck

    typeCheck -->|"user"| userValidate
    typeCheck -->|"system"| systemRoute
    systemRoute --> systemUnavailable

    userValidate -->|"否"| invalidParams
    userValidate -->|"是"| systemData
    systemData -->|"是"| unavailableData
    systemData -->|"否"| cleanText

    cleanText --> callAgent --> agentSuccess
    agentSuccess -->|"否"| agentError
    agentSuccess -->|"是"| parseOutput

    parseOutput --> outputValid
    outputValid -->|"是"| buildProfile
    outputValid -->|"否"| repair
    repair --> repairValid
    repairValid -->|"否"| invalidOutput
    repairValid -->|"是"| buildProfile

    buildProfile --> saveProfile --> success

    style typeCheck fill:#DCCCFF,stroke:#874FFF
    style saveProfile fill:#C2E5FF,stroke:#3DADFF
    style success fill:#CDF4D3,stroke:#66D575
    style invalidParams fill:#FFCDC2,stroke:#FF7556
    style unavailableData fill:#FFECBD,stroke:#FFC943
    style agentError fill:#FFCDC2,stroke:#FF7556
    style invalidOutput fill:#FFCDC2,stroke:#FF7556
    style systemUnavailable fill:#FFECBD,stroke:#FFC943
```

## 3. 路线说明

- `profile_type=user`：校验用户路线参数，调用 Agent 生成岗位画像并保存。
- `profile_type=system`：进入系统画像路线，当前返回功能未开放。
- Agent 首次输出校验失败时，只允许修复一次，防止无限重试。
- 只有通过结构校验和业务规则校验的画像才能写入数据库。

## 4. 统一响应格式

所有成功和失败响应统一使用以下结构：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

字段约定：

- `code=0` 表示成功，其他值表示业务失败。
- `msg` 表示成功提示或错误原因。
- `data` 是实际业务数据，可以是对象、列表、字符串或 `null`。
- 当前项目的 HTTP 状态码统一返回 `200`，调用方通过 `code` 判断业务是否成功。

失败响应示例：

```json
{
  "code": 422,
  "msg": "profile_type=user 时以下参数不能为空：user_id、job_text",
  "data": null
}
```
